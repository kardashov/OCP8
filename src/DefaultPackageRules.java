public class DefaultPackageRules {
	/*
	 * You should avoid using the default package except for very small example
	 * programs. From the Java language specification: It is a compile time
	 * error to import a type from the unnamed package. The only way to access
	 * classes in the default package is from another class in the default
	 * package
	 */
}
