package stas.concurrency.locks;

import java.util.concurrent.locks.*;

public class _Lock_DEMO {
	// Using a Lock object is similar to obtaining implicit locks using the
	// synchronized keyword. The aim of both constructs is the same: to ensure
	// that only one thread accesses a shared resource at a time. However,
	// unlike the synchronized keyword, Locks also support the wait/notify
	// mechanism along with its support for Condition objects.
	public static void main(String[] args) {

		ReentrantReadWriteLock f;
	}

}
